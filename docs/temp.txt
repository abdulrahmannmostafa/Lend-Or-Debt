Although all checks pass, macroeconomic features (e.g.,
GDP, CPI, TAIEX) will still be standardised using StandardScaler to ensure
numerical stability and improve model performance.          ->>>>>   transformation

///////////////-----DONE---////////////////////

------------------------------------------------------------------

Planned remediation (Phase 2): The
PAY/BILL ratio will be corrected at the 99th percentile during feature engineering
to prevent extreme values from destabilizing the tree-split thresholds. ->>>>>> cleaning

///////////////-----DONE---////////////////////
------------------------------------------------------------------

The large number of IQR outliers in PAY AMT columns is expected for financial data
exhibiting strong right-skew (occasional very large payments relative to the typical small
monthly payment). These are informational flags and will inform the log1p transformation
decision in Phase 2                 ->>>>>>>>>>>>>>> cleaning

///////////////-----DONE---////////////////////

------------------------------------------------------------------

Planned remediation (Phase 2): Apply log(1 + x)
transformation to all PAY AMT and BILL AMT columns prior to modeling ->>>>> cleaning

///////////////-----DONE---////////////////////

------------------------------------------------------------------


Distribution LIMIT BAL, AGE
non-norma --- > Power transform or scaling -->>>>>>>>>> cleaning

///////////////-----DONE---////////////////////

------------------------------------------------------------------

df["utilisation_ratio"] = df["avg_bill"] / (df["LIMIT_BAL"] + 1)

# Squared LIMIT_BAL — lets a linear model fit a curve
df["LIMIT_BAL_sq"] = df["LIMIT_BAL"] ** 2

# Interaction term — joint effect of limit and billing behaviour 
df["limit_x_bill"] = df["LIMIT_BAL"] * df["avg_bill"]

RelationshipsLIMIT BAL ↔ avg bill
non-linear (∆=0.210)
Non-linear credit
utilisation patterns
Use Spearman;
consider
polynomial
feature

--->>>>>>>>>>>>> transformation

------------------------------------------------------------------

# How much of their average bill are they actually paying back?
# This is the most direct signal of repayment discipline
df["payment_ratio"] = df["avg_payment"] / (df["avg_bill"] + 1)

# How much unpaid balance remains on average?
# Captures the accumulating debt burden
df["avg_unpaid_balance"] = df["avg_bill"] - df["avg_payment"]

# Is this borrower consistently underpaying? (binary signal)
# Underpaying means revolving debt is growing
df["is_underpaying"] = (df["avg_payment"] < df["avg_bill"]).astype(int)

Relationshipsavg bill ↔ avg payment
non-linear (∆=0.253)
Non-linear payment
behaviour
Use Spearman;
tree models
handle this
natively ->>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>transformation

------------------------------------------------------------------

pca -->>>> transformation
 
------------------------------------------------------------------

Planned reme-
diation (Phase 2): VIF analysis will be applied; either PCA will reduce the
BILL AMT columns to 1–2 principal components, or only BILL AMT1 (most re-
cent) will be retained for linear models.  -- >>>>>>>>>>>>>>>>>>>>>>>>>>> transformation


------------------------------------------------------------------